{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "prepublish": "npm run test",
    "publish": "lerna publish",
    "test": "lerna run test"
  }
}
